// project module

class Projects {
  constructor () {
    this.projects = new Set();
  }

  addProject (item) {
    this.project.add(item);
  }

  deleteProject (item) {
    this.project.delete(item);
  }
}

export default Projects;
